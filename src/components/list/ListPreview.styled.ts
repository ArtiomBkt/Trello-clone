import styled from 'styled-components'

const ListPreviewContainer = styled.div`
  position: absolute;
  inset: 0;
  margin-bottom: 8px;
  overflow-x: auto;
  overflow-y: hidden;

  background-color: green;
  font-size: 2rem;
`

export default ListPreviewContainer
